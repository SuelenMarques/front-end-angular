import { EspacoMoedaPipe } from './espaco-moeda.pipe';

describe('EspacoMoedaPipe', () => {
  it('create an instance', () => {
    const pipe = new EspacoMoedaPipe();
    expect(pipe).toBeTruthy();
  });
});
