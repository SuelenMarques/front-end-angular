import { FiltroCervejaPipe } from './filtro-cerveja.pipe';

describe('FiltroCervejaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCervejaPipe();
    expect(pipe).toBeTruthy();
  });
});
