import { PadraoBrasileiroMoedaPipe } from './padrao-brasileiro-moeda.pipe';

describe('PadraoBrasileiroMoedaPipe', () => {
  it('create an instance', () => {
    const pipe = new PadraoBrasileiroMoedaPipe();
    expect(pipe).toBeTruthy();
  });
});
